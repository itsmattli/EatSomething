# Uncomment the next line to define a global platform for your project
platform :ios, '10.3'

target 'EatSomething' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for WheresDan
  
  pod 'Alamofire', '~> 4.5'
  pod 'SwiftyJSON'
  pod 'SwiftFormat/CLI'
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'FirebaseUI/Auth'
  pod 'Firebase/Database'
  pod 'FirebaseUI'

end
